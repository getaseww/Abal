// import express, { Router } from "express";
// import NotificationController from "../controllers/notification.controller";
// import { generateToken, authentication, response } from '../../middlewares/authentication.middleware';

// let router: Router = express.Router();

// router.post("/", NotificationController.create)
//     .get("/", NotificationController.findAll)

//     .get("/:id", NotificationController.findById)

//     .put("/:id", NotificationController.update)

//     .delete("/:id", NotificationController.remove)


// export default router;